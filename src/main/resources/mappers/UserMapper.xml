<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.monfamily.wow.repository.IUserMapper">

    <insert id="userAdd">
        INSERT INTO monfamily_user
        (user_num, user_id, user_pw, user_name,
         user_mail, user_contact, last_login)
        VALUES (monfamily_user_seq.nextval,#{userId},
                #{userPw},#{userName},#{userMail},
                #{userContact},#{lastLogin})
    </insert>

</mapper>